// App.js:
// Entry point of the application that renders the top-level 'Main' component.
import Main from "./src/components/Main";

const App = () => {
  // Render the 'Main' architectural component of the app.
  return <Main />;
};

export default App;
